export enum CityToDisplay {
  Lyon = "Lyon",
  Seoul = "Seoul",
  London = "London",
  Peru = "Peru",
  Auckland = "Auckland"
}

export interface CardProps {
  title: string;
  value: string;
  image: string;
}
